get:
  operationId: getAccount
  summary: Get My Account
  tags:
    - users
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/GetUserResponse.yml
