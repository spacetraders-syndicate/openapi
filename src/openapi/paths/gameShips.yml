get:
  operationId: listGamePurchasableShips
  summary: List Game Purchasable Ships
  tags:
    - ships
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/ListGamePurchasableShipsResponse.yml
