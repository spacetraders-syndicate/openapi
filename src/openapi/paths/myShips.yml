post:
  operationId: buyUserShip
  summary: Buy User Ship
  tags:
    - ships
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../schemas/BuyUserShipPayload.yml
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/BuyUserShipResponse.yml

get:
  operationId: listUserShips
  summary: List User's Ships
  tags:
    - ships
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/ListUserShipsResponse.yml

