post:
  operationId: createUserStructure
  summary: Create User Structure
  tags:
    - structures
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../schemas/CreateUserStructurePayload.yml
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/CreateUserStructureResponse.yml

get:
  operationId: listUserStructures
  summary: List User's Structures
  tags:
    - structures
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/ListUserStructuresResponse.yml
