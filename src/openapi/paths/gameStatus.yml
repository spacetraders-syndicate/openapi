get:
  operationId: getGameStatus
  summary: Get Game Status
  tags:
    - game
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/GetGameStatusResponse.yml
