get:
  operationId: getUserShip
  summary: Get User's Ship Info
  tags:
    - ships
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/GetUserShipResponse.yml

delete:
  operationId: scrapUserShip
  summary: Scrap User's Ship
  tags:
    - ships
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/ScrapUserShipResponse.yml

parameters:
  - $ref: ../schemas/ParameterUsername.yml
  - $ref: ../schemas/ParameterShipId.yml
