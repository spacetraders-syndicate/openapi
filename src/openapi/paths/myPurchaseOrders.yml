post:
  operationId: createUserPurchaseOrder
  summary: Create a New Purchase Order
  tags:
    - purchase orders
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../schemas/CreateUserPurchaseOrderPayload.yml
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/CreateUserPurchaseOrderResponse.yml

