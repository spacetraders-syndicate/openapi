get:
  operationId: listGameLoans
  summary: List Game Loans
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/ListGameLoansResponse.yml
