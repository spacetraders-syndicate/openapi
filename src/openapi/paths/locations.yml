get:
  operationId: getGameLocation
  summary: Get Game Location
  tags:
     - locations
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/GetGameLocationResponse.yml

parameters:
  - $ref: ../schemas/ParameterSymbol.yml
