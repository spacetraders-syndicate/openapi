post:
  operationId: jettisonShipCargo
  summary: Jettison Ship Cargo
  tags:
    - ships
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../schemas/JettisonShipCargoPayload.yml
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/JettisonShipCargoResponse.yml

parameters:
  - $ref: ../schemas/ParameterShipId.yml
