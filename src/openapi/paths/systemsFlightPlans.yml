get:
  operationId: listSystemFlightPlans
  summary: List Flight Plans
  tags:
    - flight plans
    - systems
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/ListGameSystemFlightPlansResponse.yml

parameters:
  - $ref: ../schemas/ParameterSymbol.yml
