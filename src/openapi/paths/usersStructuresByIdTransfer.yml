post:
  operationId: transferStructureIenventory
  summary: Transfer Structure Inventory
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../schemas/TransferStructureInventoryPayload.yml
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/TransferStructureInventoryResponse.yml

parameters:
  - $ref: ../schemas/ParameterUsername.yml
  - $ref: ../schemas/ParameterStructureId.yml
