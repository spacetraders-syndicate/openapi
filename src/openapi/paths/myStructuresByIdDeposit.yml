post:
  operationId: depositStructureIenventory
  summary: Deposit Structure Inventory
  tags:
    - structures
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../schemas/DepositStructureInventoryPayload.yml
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/DepositStructureInventoryResponse.yml

parameters:
  - $ref: ../schemas/ParameterStructureId.yml
