get:
  operationId: getUserStructure
  summary: Get User's Structure Info
  tags:
    - structures
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/GetUserStructureResponse.yml

parameters:
  - $ref: ../schemas/ParameterStructureId.yml
