get:
  operationId: getUser
  summary: Get User
  tags:
    - users
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/GetUserResponse.yml

parameters:
  - $ref: ../schemas/ParameterUsername.yml
