get:
  operationId: getUserFlightPlan
  summary: Get a User Flight Plan
  tags:
    - flight plans
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/GetUserFlightPlanResponse.yml

parameters:
  - $ref: ../schemas/ParameterUsername.yml
  - $ref: ../schemas/ParameterFlightPlanId.yml
