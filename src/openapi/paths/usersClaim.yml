post:
  operationId: createUsersClaim
  summary: Claim Username and Get Token
  tags:
    - users
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schemas/CreateUserTokenResponse.yml

parameters:
  - $ref: ../schemas/ParameterUsername.yml
